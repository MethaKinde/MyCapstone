@using MyChurch.Models
@model List<User>

@{ 
    ViewBag.Title = "Index";
}

<div class="container" style="margin-top: 85px;">
    <h2>Users</h2>

    <ul>
        @foreach (var user in Model)
            {
                <li>@user.UserName</li>
            }
    </ul>
</div>